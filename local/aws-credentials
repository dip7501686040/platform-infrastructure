# Floci accepts any value here -- these are its fixed dummy credentials,
# the same for every user, not a real secret. Copy this file to
# local/aws-credentials as-is.
[floci]
aws_access_key_id = test
aws_secret_access_key = test
